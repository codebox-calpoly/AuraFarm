{
  "private": true,
  "scripts": {
    "postinstall": "cd frontend && npm install && cd ../backend && npm install && npm run build",
    "start": "cd frontend && npx expo start -c",
    "start:web": "cd frontend && npx expo start --web",
    "dev:backend": "cd backend && npm run dev"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/react": "~19.1.10",
    "jest": "^30.2.0",
    "ts-jest": "^29.4.6"
  },
  "dependencies": {
    "expo": "^54.0.32",
    "react-dom": "19.1.0",
    "react-native-web": "^0.21.0"
  }
}